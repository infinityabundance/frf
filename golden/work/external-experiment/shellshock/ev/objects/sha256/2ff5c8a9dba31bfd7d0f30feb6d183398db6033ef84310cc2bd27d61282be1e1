#!/bin/sh
# FRF court-challenge mutant: stdout-first-line of objects/sha256/7bb670057ae6a25086e789761e2cd275a28557eb872b84fae766ecb12b60af6c
self_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd) || exit 2
ref="$self_dir/7bb670057ae6a25086e789761e2cd275a28557eb872b84fae766ecb12b60af6c"
out=$(mktemp) || exit 2
err=$(mktemp) || exit 2
"$ref" "$@" >"$out" 2>"$err"
rc=$?
{ IFS= read -r first; printf '%s\n' "FRF-MUTANT:stdout-first-line:${first}"; cat; } <"$out"
cat "$err" >&2
rm -f "$out" "$err"
exit "$rc"
