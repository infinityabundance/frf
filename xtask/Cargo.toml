[package]
name = "xtask"
version = "0.1.20"
edition = "2021"
description = "The independent FRF verifier — a deliberately small SECOND implementation of the FRF protocol, with no dependency on the frf reference engine."
license = "MIT OR Apache-2.0"
publish = false
rust-version = "1.85"

[dependencies]
serde = "1"
serde_json = "1"
serde_yaml = "0.9"
sha2 = "0.10"
